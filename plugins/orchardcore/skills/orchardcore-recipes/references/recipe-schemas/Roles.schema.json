{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "const": "Roles"
    },
    "Roles": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "Name": {
            "type": "string"
          },
          "Description": {
            "type": "string"
          },
          "Permissions": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "enum": [
              "PermissionA",
              "PermissionB"
            ]
          },
          "PermissionBehavior": {
            "type": "string",
            "enum": [
              "Add",
              "Replace",
              "Remove"
            ],
            "description": "How permissions are merged: Add (default), Replace, or Remove."
          }
        },
        "required": [
          "Name"
        ],
        "additionalProperties": true
      },
      "minItems": 1
    }
  },
  "required": [
    "name",
    "Roles"
  ],
  "additionalProperties": true
}