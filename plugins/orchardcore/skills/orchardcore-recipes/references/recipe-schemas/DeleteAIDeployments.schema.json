{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "const": "DeleteAIDeployments"
    },
    "IncludeAll": {
      "type": "boolean",
      "description": "When true, all deployments will be removed."
    },
    "DeploymentNames": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Specific deployment names to delete. Ignored when IncludeAll is true."
    }
  },
  "required": [
    "name"
  ],
  "additionalProperties": true
}