{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "const": "recipes",
      "description": "Recipe step discriminator. Must be \u0027recipes\u0027."
    },
    "Values": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "executionid": {
            "type": "string",
            "description": "Unique execution identifier used to track the nested recipe execution."
          },
          "name": {
            "type": "string",
            "enum": [],
            "description": "Name of the non-startup recipe to execute."
          }
        },
        "required": [
          "executionid",
          "name"
        ],
        "additionalProperties": true
      },
      "description": "Referenced recipes to execute from this step."
    }
  },
  "required": [
    "name",
    "Values"
  ],
  "additionalProperties": true
}